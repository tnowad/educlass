query MyCoursesQuery($first: Int, $after: String) {
  courses(first: $first, after: $after, where: { userId: "me" }) {
    edges {
      node {
        id
        name
        code
      }
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
